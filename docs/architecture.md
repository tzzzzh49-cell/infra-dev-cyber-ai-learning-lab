# Architecture

Ce fichier historique pointait vers l'ancien lab réseau/FastAPI.
Le contenu de référence du projet est maintenant `docs/ARCHITECTURE.md`.

L'ancien lab n'est pas supprimé brutalement : les fichiers `app/`, `ansible/`, `openclaw/`, `outputs/` et certains scripts historiques restent dans le dépôt pour une migration prudente.
Une étape future décidera s'ils doivent être adaptés, déplacés vers `docs/legacy/` ou retirés proprement.
